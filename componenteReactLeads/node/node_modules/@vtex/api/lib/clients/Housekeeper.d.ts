import { InfraClient, InstanceOptions } from '../HttpClient';
import { HousekeeperStatesAndUpdates } from '../responses';
import { IOContext } from '../service/typings';
export declare class Housekeeper extends InfraClient {
    private _routes;
    private readonly routes;
    constructor(context: IOContext, options?: InstanceOptions);
    apply: (data: HousekeeperStatesAndUpdates) => Promise<void>;
    perform: () => Promise<void>;
    resolve: () => Promise<HousekeeperStatesAndUpdates>;
}
