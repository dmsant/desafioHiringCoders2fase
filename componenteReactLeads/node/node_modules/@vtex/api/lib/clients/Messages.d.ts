import { AppClient, InstanceOptions } from '../HttpClient';
import { IOContext } from '../service/typings';
import { IOMessage } from '../utils/message';
interface Locale {
    [token: string]: string;
}
interface Locales {
    [lang: string]: Locale;
}
interface LocalesByProvider {
    [provider: string]: Locales;
}
export declare class Messages extends AppClient {
    constructor(vtex: IOContext, options?: InstanceOptions);
    translate: (to: string, data: IOMessage[]) => Promise<string[]>;
    saveTranslation: (data: LocalesByProvider) => Promise<void>;
}
export {};
