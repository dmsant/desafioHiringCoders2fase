import { IOClients } from '../../../clients/IOClients';
import { VaryHeaders } from '../../../constants';
import { ServiceContext } from '../../typings';
export interface CachingStrategy {
    forbidden: VaryHeaders[];
    path: string;
    vary: VaryHeaders[];
}
export declare const cachingStrategies: CachingStrategy[];
export declare function cdnNormalizer<T extends IOClients, U, V>(ctx: ServiceContext<T, U, V>, next: () => Promise<any>): Promise<void>;
