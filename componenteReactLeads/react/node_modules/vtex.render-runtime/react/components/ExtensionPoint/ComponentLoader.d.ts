import { FunctionComponent } from 'react';
export declare function fetchComponent(component: string, runtimeFetchComponent: RenderContext['fetchComponent'], retries?: number): Promise<any>;
interface Props {
    component: string | null;
    props: Record<string, any>;
    treePath: string;
    runtime: RenderContext;
    hydration: Hydration;
}
declare const ComponentLoader: FunctionComponent<Props>;
export default ComponentLoader;
