interface Options {
    max?: number;
    maxSize?: number;
    maxAge?: number;
    disposeFn?: (key: string) => void;
}
declare class LRU<T> {
    private map;
    private maxSize;
    private maxAge;
    private disposeFn;
    private head;
    private tail;
    constructor(options: Options);
    private isExpired;
    private ensureLimit;
    private remove;
    get(key: string): T | undefined;
    has(key: string): boolean;
    set(key: string, value: T, nodeTime?: number): void;
}
export default LRU;
