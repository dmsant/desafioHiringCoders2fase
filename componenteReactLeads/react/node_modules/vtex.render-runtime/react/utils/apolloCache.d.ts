export interface QueryData {
    data: string;
    query: string;
    variables: Record<string, any>;
}
export declare const createHydrationFn: (client: ApolloClientType, warningMessage?: string) => (queryData: QueryData[] | undefined) => Promise<void>;
