import { ApolloLink } from 'apollo-link';
import { RenderProvider } from '../../../components/RenderProvider';
export declare function createRuntimeContextLink(initialRuntime: RenderRuntime): {
    setRenderProviderInstance: (instance: RenderProvider) => void;
    link: ApolloLink;
};
