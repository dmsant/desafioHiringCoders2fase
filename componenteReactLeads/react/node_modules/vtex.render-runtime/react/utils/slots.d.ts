import React from 'react';
interface GenerateSlotArgs {
    treePath: string;
    slotName: string;
    slotValue: string;
    hydration: Hydration;
}
export declare function generateSlot({ treePath, slotName, slotValue, hydration, }: GenerateSlotArgs): React.FC<any>;
export {};
